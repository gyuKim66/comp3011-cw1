# Frontend scaffold

This template includes only a minimal API client placeholder.

If you haven't created the Next.js app yet, run:

```bash
cd frontend
npx create-next-app@latest . --ts --eslint --app
npm run dev
```

Then use `services/api.ts` to call the backend.
